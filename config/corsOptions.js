const corsOptions = {
  credentials: true,
  origin: "https://catena-react-admin-module.vercel.app",
  // origin: (origin, callback) => {
  //   callback(null, true);
  // },
};

export default corsOptions;
